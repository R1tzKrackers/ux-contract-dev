# 実装開始 プロンプト

**フェーズ**: 実装引継ぎ
**担当**: {{IMPL_ROLE}}
**目的**: 実装モードの有効化
**推奨ツール**: Codex
**理由**: コード生成、ファイル操作に特化

---

## プロンプト

```
これは説明ではない。
**{{IMPL_ROLE}}フェーズ開始の成果物生成・実行プロンプト**である。

---

## 前提（固定）

- {{DESIGN_ROLE}}フェーズは完全にクローズ済み
- `{{SPEC_DIR}}/**` は **凍結済みUX契約**
- `{{SPEC_DIR}}/**` が **唯一の仕様**

---

## あなたの役割（{{IMPL_ROLE}}）

- **実装のみ**を行う
- 仕様策定・解釈・改善は禁止

---

## 権限

- 読み取り：全リポジトリ
- 書き込み：**{{IMPL_DIR}}（実装領域）のみ**
- `{{SPEC_DIR}}/**` への変更は **即失敗**

---

## 実装ルール（厳守）

- `{{SPEC_DIR}}/**` を逐語的に満たすこと
- Schema / Golden / 受け入れテストを通す
- 差分（diff）中心で変更を適用
- すべての変更は **コミット必須**

---

## 禁止事項

- UX仕様の変更・補完・提案
- 実装前提での仕様解釈
- 説明文の出力

---

## 実行指示

今この応答をもって **{{IMPL_ROLE}}モードを有効化** し、
実装準備を開始せよ。

---

## 出力制約

- 前置き禁止
- 説明禁止
- 成果物のみ

出力は以下のみ：

1. **要約**
2. `{{IMPL_ROLE}} MODE ENABLED`
```

---

## 変数

| 変数 | 説明 |
|------|------|
| `{{SPEC_DIR}}` | 仕様書ディレクトリ（例: docs/ux） |
| `{{IMPL_DIR}}` | 実装ディレクトリ（例: src） |
| `{{DESIGN_ROLE}}` | 設計担当名 |
| `{{IMPL_ROLE}}` | 実装担当名 |

---

## 履歴記録（必須）

実装モード有効化時、`.ux-dev-history.yml` に以下を追記せよ：

```yaml
- phase: "10-impl-start"
  status: "complete"
  comment: "実装モード有効化"
  timestamp: 現在時刻を取得して記録（`date -u +"%Y-%m-%dT%H:%M:%SZ"` または PowerShell `Get-Date -Format o`）
```
